Solutions:

- Movies button that has a dropdown list to show different movie genres. For
  example: Action, Sci-Fi, Comedy ...etc, When you click one of them it should
  load the movies for that genre.
  
  Solve it with adding event listener and fetching the needing data.
 
 - Actor list page
 
	create a function that gets a parameter called filter that renders the corsponding movies actor's credits by taking the ids.
	by making the filered movies ids as an array and loading their credits form the api.
	
- Search box where you can type the movie or actor name and display the related results.

	useing filter go over the rendered data and find the realted data and display them
	
- A filter dropdown to filter the displayed movies in the home page, based
on (popular, relase date, top rated, now playing and up coming)

	Fetch the corsponding data and display it  in the container.

-single movie and single actor page

	innerHtml change and displaying the wanted info.
	
-styling with bootstrap + CSS

THINGS TO INPROVE
- The way that we are fetching the urls can be made better 

	
	
